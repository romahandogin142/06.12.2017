#include <iostream>
#include <stdio.h>
#include <conio.h>
#include <locale.h>
using namespace std; 
int main()
{
	setlocale(LC_ALL, "Rus");
	int nz = 0;
start:
	
		cout << "Ведите номер задания";
		cin >> nz;
		if (nz == 1)
		{































		}
		else if (nz >= 0)
		{
			cout << "Такого задание нету , повторите пожалуйста попытку \n";

		}
	system("pause");
	//getch();
	return 0;
}